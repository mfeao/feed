// import { reducers as pkgReducers } from '@dist/cjs';
// import { reducers as pkgReducers } from '@dist/esm';
import { reducers as pkgReducers } from '@package';

const reducers = {
  ...pkgReducers,
};

export default reducers;
